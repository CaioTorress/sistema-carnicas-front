/** Tipos de arquivo que o bulk upload identifica (rótulos na interface). */
export const DOCUMENT_KINDS_SHORT_LABEL = 'AATIPP / CR / BOLETO'

export const DOCUMENT_KINDS_LIST_LABEL = 'AATIPP, CR e boleto'

/** Extensões aceitas no upload de documentos (PDF + imagens para boleto). */
export const DOCUMENT_FILE_ACCEPT = '.pdf,.png,.jpg,.jpeg,.webp'
