/** Tipos de arquivo que o bulk upload identifica (rótulos na interface). */
export const DOCUMENT_KINDS_SHORT_LABEL = 'AATIPP / CR / BOLETO / NFS-e'

export const DOCUMENT_KINDS_LIST_LABEL = 'AATIPP, CR, boleto e NFS-e'

/** Extensões aceitas no upload de documentos (PDF, imagens, XML de NFS-e). */
export const DOCUMENT_FILE_ACCEPT = '.pdf,.png,.jpg,.jpeg,.webp,.xml'
